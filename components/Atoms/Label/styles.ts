import { LabelVariants } from '@/components/Atoms/Label/types'

export const defaultStyles = 'relative cursor-text transition-all'

export const variants: Record<LabelVariants, string> = {
  primary: 'text-sm text-primary/50',
  secondary: '',
}
